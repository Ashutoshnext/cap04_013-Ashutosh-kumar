const add=(a,b)=>{
    return a+b;
}
const multiply=(a,b)=>{
    return a*b;
}

let addans=add(2,3);
console.log('add :',addans)

const multiplyans=multiply(3,4);
console.log('multiply :',multiplyans)
